<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/nav_home">

    <!-- Profile Fragment -->
    <fragment
        android:id="@+id/nav_profile"
        android:name="com.example.valetparking.ProfileFragment"
        android:label="Profile"
        tools:layout="@layout/fragment_profile" />

    <!-- Home Fragment -->
    <fragment
        android:id="@+id/nav_home"
        android:name="com.example.valetparking.HomeFragment"
        android:label="HomeFragment" >
    </fragment>

    <!-- Spot Fragment -->
    <fragment
        android:id="@+id/nav_spot"
        android:name="com.example.valetparking.SpotFragment"
        android:label="SpotFragment" >
    </fragment>

    <!-- Update Profile Fragment -->
    <fragment
        android:id="@+id/updateProfileFragment"
        android:name="com.example.if570_lab_uts_merhandesgunawan_00000081070.UpdateProfileFragment"
        android:label="UpdateProfileFragment" />

</navigation>
